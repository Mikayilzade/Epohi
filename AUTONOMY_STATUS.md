# AUTONOMY STATUS — «Эпохи» Humans v1

## State
`IN_PROGRESS_AUTONOMOUS`

## Integration
- Repository: `Mikayilzade/Epohi`
- Active branch: `codex/coherence-capture-learning-v1`
- Draft PR: #84
- Base: `prototype/humans-v1`
- `main`: DO NOT TOUCH

## Current checkpoint
Latest implementation checkpoint: `6273417945b098fefa9cd3c3a10c63c7238bf438` (`Stabilize joint-war real-turn regression`).

The exact preceding implementation checkpoint `7912bd47ec61910c15668fa2329ed1ee62414a9c` (`Bound runtime invalidation flush cadence`) ran as CI `32890787000` (run #156) and completed **failure**. Its artifact `9579511917` was inspected before the new test checkpoint was written.

## Exact CI / validation for `7912bd47...`
- Chromium focused: **54/55 passed, 1 failed**.
- WebKit focused: **53/55 passed, 2 failed**.
- The earlier runtime callback blockers are closed in this run: the selected-worker / repeated-city callback limits and the bounded invalidation cadence tests pass on both focused projects.
- Shared Chromium + WebKit failure: `tests/combat-world-stability.spec.js:184`, `allied joint-war proposal is generated by a real turn only before either side joins`, hit the 20-second test timeout while a two-rival turn was still consuming the test budget.
- WebKit-only failure: `tests/humans-strategy-ux.spec.js:19`, because Playwright mobile WebKit does not support `mouse.wheel`.
- Full Chromium/WebKit regression was skipped because the focused gate failed.

## Bounded package in `62734179...`
The joint-war regression still exercises proposal generation through a real end-turn, but removes unrelated pathfinding/combat cost from the fixture by clearing both rival unit arrays and barbarians. It also waits for an actual turn increment plus `!isTurnProcessing()` before inspecting the proposal, instead of relying only on the processing flag. Gameplay/source semantics, callback thresholds, and actionability limits are unchanged.

This checkpoint is test-only. It is intended to distinguish the real diplomacy assertion from unrelated AI-turn workload and eliminate the shared Chromium/WebKit timeout without weakening the behavior being tested.

## Current blocker
The exact Chromium/WebKit result for implementation checkpoint `6273417945b098fefa9cd3c3a10c63c7238bf438` is not yet recorded. Do not push another source/test/runtime fix until that automatically-triggered PR CI is complete and inspected.

## Phase plan
- [x] Phase 0A — autonomous control plane and quality gates.
- [x] Phase 0B — PR CI for Chromium + WebKit mobile projects and full regression.
- [ ] Phase 1 — runtime/UI architecture hardening; remove observer/decorator feedback cycles.
- [ ] Phase 2 — complete cross-browser functional regression and save/migration coverage.
- [ ] Phase 3 — autonomous soak player; deterministic multi-seed long-run campaigns.
- [ ] Phase 4 — automated balance/UX/layout pass from soak telemetry and screenshots.
- [ ] Phase 5 — RC cleanup, exact immutable build, one physical iPhone playthrough.

## NEXT ACTION
Fetch PR #84 and inspect the exact automatically-triggered Chromium/WebKit CI for implementation checkpoint `6273417945b098fefa9cd3c3a10c63c7238bf438`. If the joint-war real-turn regression is green on both projects, treat the first remaining factual focused failure as the next blocker; expected candidate is the unsupported mobile-WebKit `mouse.wheel` scenario. Make no new source/test/runtime push while the current checkpoint CI is in progress, and do not weaken any runtime/actionability threshold.

## Completion signal
Change state to `READY_FOR_FINAL_DEVICE_TEST` only after all applicable gates in `QUALITY_GATES.md` are green and the branch has been cleaned into a Release Candidate. Do not merge automatically.
